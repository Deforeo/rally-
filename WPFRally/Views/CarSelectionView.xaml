<UserControl x:Class="WPFRally.Views.CarSelectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFRally.Views">
    <Grid Background="{StaticResource MenuBackground}">
    <StackPanel>
        <TextBlock Text="ВЫБЕРИТЕ АВТОМОБИЛЬ" Style="{StaticResource TitleText}"/>
            <ListBox ItemsSource="{Binding Cars}" Height="300" Margin="20">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Name}" Width="Auto" Style="{StaticResource NormalText}" Foreground="#2C3E50"/>
                            <Button Content="Выбрать" 
        Command="{Binding DataContext.SelectCarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
        CommandParameter="{Binding}" 
        Width="150"/>
                        </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Назад" Command="{Binding BackCommand}" Width="150" Margin="10"/>
    </StackPanel>
    </Grid>
</UserControl>
