<UserControl x:Class="WPFRally.Views.TrackSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource MenuBackground}">
        <StackPanel>
            <TextBlock Text="ВЫБЕРИТЕ ТРАССУ" Style="{StaticResource TitleText}"/>
            <ListBox ItemsSource="{Binding Tracks}" Height="300" Margin="20">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Name}" Width="Auto" Style="{StaticResource NormalText}" Foreground="#2C3E50"/>
                            <Button Content="Выбрать" 
                                    Command="{Binding DataContext.SelectTrackCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}" 
                                    Width="150"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="Назад" Command="{Binding BackCommand}" Width="150" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>